library project Deepend_Lib is

   for Languages use ("Ada");
   for Library_Name use "Deepend";
   for Object_Dir use "obj";
   for Library_Dir use "lib";
   for Library_Kind use "static";
   for Source_Dirs use ("src");

   package Compiler is
      for Switches ("ada") use ("-gnatf", "-gnata", "-gnat2005", "-gnatwa.f.g.kh.o_p", "-gnatyaAbBcdDefhiIklM79noOprStux");
   end Compiler;

end Deepend_Lib;

